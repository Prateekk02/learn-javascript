"use strict"; // treat all js code as newer version.

// alert(3+3)  we are using node.js not the browser hence this line gives error. 
// Follow good code writing habbits.

// Javascript's original documentation --> tc39.es  (ECMA script)


// null --> standalone value.

//symbol --> used during react (unique)

//object.

console.log(typeof null);   // returns datatype as object !! 
console.log(typeof undefined);